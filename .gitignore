# Internal Frontier strategy document — do NOT publish
# (the thesis .docx is the only Word doc in the folder; this keeps it out of the public repo)
*.docx

# Internal working notes — planned improvements backlog (not published)
to_do.md

# OS / editor cruft
.DS_Store
*.swp
Thumbs.db

# Python
__pycache__/
*.pyc
.venv/
venv/

# US county map — large raw source downloads (re-fetch with scripts/us/download_raw.sh)
data/geo/us_raw/

# EU NUTS-2 map — large raw source downloads (re-fetch with scripts/eu/download_raw.sh)
data/geo/eu_raw/

# Canada CD map — large raw source downloads (re-fetch with scripts/ca/download_raw.sh)
data/geo/ca_raw/

# Transport model — NTAD raw download + searoute cache (re-fetch/rebuild with download_raw.sh)
data/geo/transport_raw/

# AD gating — EPA AgSTAR raw download (re-fetch with scripts/us/download_raw.sh)
data/geo/ad_raw/
